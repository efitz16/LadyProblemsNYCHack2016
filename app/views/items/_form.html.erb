<%= form_for [@bill, @item] do |f| %>
  <% if @item.errors.any? %>
    <ul>
      <% @item.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  <% end %>
<div>Areas with asterisks are mandatory.</div>
  <p>
   <div>*Item Name </div>
   You don't have to follow the item description to the letter! Write it in a way that others can understand - no need for medical-babble!<br>
  	<%= f.text_field :item_name %>
  </p>

  <p>
    <%= f.label :price %>
    $<%= f.number_field :price, step: :any %>
  </p>

  <p>
    <%= f.label :quantity %>
    <%= f.number_field :quantity %>
  </p>

  <p>
    <%= f.submit "Submit Item" %>
  </p>

<% end %>
