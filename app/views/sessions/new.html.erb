<div class="container">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1">
      <h1 class="text-center col-sm-offset-1">Login</h1>
        <%if flash[:notice]%>
          <div class="text-center"><%=flash[:notice]%></div>
        <%end%>
        <div id="loginform">
          <%= bootstrap_form_for(:session, url:login_path, label_errors:true, layout: :horizontal, label_col: "col-sm-3", control_col: "col-sm-7") do |f| %>
            <%=f.email_field :email%>
            <%=f.password_field :password%>
            <%= f.form_group do %>
              <%=f.submit "log in", :class =>"btn btn-primary center-block"%>
            <%end%>
          <%end%>
        </div>
        <p class="text-center"><%=link_to "Don't have an account? Sign up!", new_user_path%></p>
    </div>
  </div>
</div>
