<div class="container">
  <h1 class='text-center'> Edit </h1>
  <% provide(:button_text, 'Save') %>
  <%= render 'form'%>
    <div class="text-center delete-btn"><%=link_to "delete account", @user, method: :delete, data: { confirm: "Are you sure you want to delete your account?" }%></div>
</div>